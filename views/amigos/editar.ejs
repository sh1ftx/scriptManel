<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Editar Amigo • Sistema</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        * {
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            flex-direction: column;

            background: linear-gradient(
                135deg,
                #fbc2eb,
                #a6c1ee,
                #cfd9df
            );
            background-size: 300% 300%;
            animation: gradientMove 14s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .glass-card {
            width: 100%;
            max-width: 520px;
            padding: 2.5rem;

            border-radius: 28px;
            background: rgba(255, 255, 255, 0.65);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);

            box-shadow:
                0 25px 50px rgba(0, 0, 0, 0.12),
                inset 0 0 0 1px rgba(255, 255, 255, 0.35);
        }

        .title {
            font-weight: 700;
            text-align: center;
            margin-bottom: 0.5rem;

            background: linear-gradient(
                90deg,
                #667eea,
                #764ba2
            );
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .subtitle {
            text-align: center;
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 2rem;
        }

        label {
            font-size: 0.85rem;
            font-weight: 600;
            color: #555;
        }

        .form-control {
            border-radius: 14px;
            padding: 0.65rem 0.9rem;
            border: 1px solid #ddd;
        }

        .form-control:focus {
            border-color: #a6c1ee;
            box-shadow: 0 0 0 0.15rem rgba(166, 193, 238, 0.35);
        }

        .btn-save {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: #fff;
            border: none;
            border-radius: 16px;
            padding: 0.7rem;
            font-weight: 600;
            transition: all 0.25s ease;
        }

        .btn-save:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(118, 75, 162, 0.35);
        }

        .btn-cancel {
            border-radius: 16px;
            background: rgba(255, 255, 255, 0.85);
            border: 1px solid #ddd;
            color: #555;
            padding: 0.7rem;
            font-weight: 500;
        }

        .btn-cancel:hover {
            background: rgba(255, 255, 255, 1);
        }
    </style>
</head>

<body>

    <% if (erro) { %>
        <div class="alert alert-warning mt-3">
            <strong>Atenção:</strong> <%= erro %>
        </div>
    <% } %>

    <div class="glass-card">
        <h2 class="title">Editar Amigo</h2>
        <p class="subtitle">Atualize os dados do contato</p>

        <form action="/amigos/editar/<%= amigo.id %>" method="POST">
            <div class="mb-3">
                <label>Nome</label>
                <input
                    type="text"
                    name="nome"
                    class="form-control"
                    value="<%= amigo.nome %>"
                    required
                >
            </div>

            <div class="mb-4">
                <label>Email</label>
                <input
                    type="email"
                    name="email"
                    class="form-control"
                    value="<%= amigo.email %>"
                    required
                >
            </div>

            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-save">
                    Salvar Alterações
                </button>

                <a href="/amigos" class="btn btn-cancel">
                    Cancelar
                </a>
            </div>
        </form>
    </div>

</body>
</html>
