<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Novo Empréstimo • Sistema</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        * {
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;

            background: linear-gradient(
                135deg,
                #fbc2eb,
                #a6c1ee,
                #b5ead7
            );
            background-size: 300% 300%;
            animation: gradientMove 12s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .form-card {
            width: 100%;
            max-width: 620px;
            padding: 2.5rem;

            border-radius: 24px;
            background: rgba(255, 255, 255, 0.65);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);

            box-shadow:
                0 20px 45px rgba(0, 0, 0, 0.12),
                inset 0 0 0 1px rgba(255, 255, 255, 0.3);
        }

        .form-title {
            font-weight: 700;
            letter-spacing: 0.5px;
            background: linear-gradient(
                90deg,
                #7f7fd5,
                #86a8e7,
                #91eae4
            );
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .form-subtitle {
            font-size: 0.95rem;
            color: #6c757d;
        }

        label {
            font-size: 0.85rem;
            font-weight: 500;
            color: #555;
        }

        .form-control,
        .form-select {
            border-radius: 14px;
            padding: 0.75rem 1rem;
            border: 1px solid #e3e3e3;
            background-color: rgba(255, 255, 255, 0.85);
            transition: all 0.25s ease;
        }

        .form-control:focus,
        .form-select:focus {
            border-color: #91eae4;
            box-shadow: 0 0 0 0.2rem rgba(145, 234, 228, 0.4);
        }

        .btn-primary-soft {
            border: none;
            border-radius: 16px;
            padding: 0.75rem;

            font-weight: 600;
            letter-spacing: 0.4px;
            color: #fff;

            background: linear-gradient(
                135deg,
                #43cea2,
                #185a9d
            );
            background-size: 200% 200%;
            transition: all 0.3s ease;
        }

        .btn-primary-soft:hover {
            transform: translateY(-2px);
            background-position: right center;
            box-shadow: 0 10px 25px rgba(67, 206, 162, 0.35);
        }

        .btn-cancel {
            border-radius: 16px;
            padding: 0.75rem;
            font-weight: 500;

            background: rgba(255, 255, 255, 0.7);
            border: 1px solid #ddd;
            color: #555;
            transition: all 0.25s ease;
        }

        .btn-cancel:hover {
            background: rgba(255, 255, 255, 0.9);
            transform: translateY(-1px);
        }
    </style>
</head>

<body>

    <div class="form-card">
        <div class="text-center mb-4">
            <h3 class="form-title">Registrar Empréstimo</h3>
            <p class="form-subtitle">Controle de empréstimos de forma simples</p>
        </div>

        <form action="/emprestimos/novo" method="POST">
            <div class="mb-3">
                <label class="form-label">Qual jogo?</label>
                <select name="jogoId" class="form-select" required>
                    <% jogos.forEach(jogo => { %>
                        <option value="<%= jogo.id %>"><%= jogo.titulo %></option>
                    <% }) %>
                </select>
            </div>

            <div class="mb-3">
                <label class="form-label">Quem vai pegar?</label>
                <select name="amigoId" class="form-select" required>
                    <% amigos.forEach(amigo => { %>
                        <option value="<%= amigo.id %>"><%= amigo.nome %></option>
                    <% }) %>
                </select>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">Data Início</label>
                    <input type="date" name="dataInicio" class="form-control" required>
                </div>

                <div class="col-md-6 mb-4">
                    <label class="form-label">Data Devolução (Opcional)</label>
                    <input type="date" name="dataFim" class="form-control">
                </div>
            </div>

            <div class="d-grid gap-3">
                <button type="submit" class="btn btn-primary-soft">
                    Emprestar
                </button>
                <a href="/emprestimos" class="btn btn-cancel">
                    Cancelar
                </a>
            </div>
        </form>
    </div>

</body>
</html>
