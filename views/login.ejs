<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Login • Sistema de Jogos</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        * {
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;

            background: linear-gradient(
                135deg,
                #f5f6f8,
                #e9ebf0,
                #f1dfe7
            );
            background-size: 300% 300%;
            animation: gradientMove 14s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .login-card {
            width: 100%;
            max-width: 420px;
            padding: 2.6rem;

            border-radius: 24px;
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);

            box-shadow:
                0 25px 50px rgba(0, 0, 0, 0.12),
                inset 0 0 0 1px rgba(255, 255, 255, 0.35);
        }

        .login-title {
            font-weight: 800;
            letter-spacing: 0.6px;
            color: #3a4a5a;
        }

        .login-subtitle {
            font-size: 0.95rem;
            color: #6b6f75;
        }

        label {
            font-size: 0.85rem;
            font-weight: 500;
            color: #555;
        }

        .form-control {
            border-radius: 14px;
            padding: 0.75rem 1rem;
            border: 1px solid #dedede;
            background-color: rgba(255, 255, 255, 0.95);
            transition: all 0.25s ease;
        }

        .form-control:focus {
            border-color: #b0b7c3;
            box-shadow: 0 0 0 0.2rem rgba(176, 183, 195, 0.35);
        }

        .btn-login {
            border: none;
            border-radius: 16px;
            padding: 0.85rem;

            font-weight: 600;
            letter-spacing: 0.4px;
            color: #fff;

            background: linear-gradient(
                135deg,
                #5f6f82,
                #d6b1c5
            );
            background-size: 200% 200%;
            transition: all 0.3s ease;
        }

        .btn-login:hover {
            background-position: right center;
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(95, 111, 130, 0.35);
            color: #fff;
        }

        .alert {
            border-radius: 14px;
            font-size: 0.9rem;
        }

        .login-footer {
            font-size: 0.8rem;
            color: #777;
        }

        .login-footer b {
            color: #555;
        }
    </style>
</head>

<body>

    <div class="login-card">
        <div class="text-center mb-4">
            <h3 class="login-title">Realize seu login</h3>
            <p class="login-subtitle">Entre com suas credenciais</p>
        </div>

        <% if (locals.erro) { %>
            <div class="alert alert-danger text-center" role="alert">
                <%= erro %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            <div class="mb-3">
                <label class="form-label">Usuário</label>
                <input type="text" name="email" class="form-control" required autofocus>
            </div>

            <div class="mb-4">
                <label class="form-label">Senha</label>
                <input type="password" name="senha" class="form-control" required>
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-login">
                    Entrar
                </button>
            </div>
        </form>

        <div class="text-center mt-4 login-footer">
            Dica: Use <b>adm</b> / <b>123</b>
        </div>
    </div>

</body>
</html>
