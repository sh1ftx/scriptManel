<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Jogos • Dados Completos</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        * {
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        }

        /* FUNDO AZUL PASTEL */
        body {
            min-height: 100vh;
            padding: 3rem 2rem;

            background: linear-gradient(
                135deg,
                #e9f0ff,
                #dfe9ff,
                #d6e4ff,
                #e0f2ff
            );
            background-size: 400% 400%;
            animation: gradientMove 18s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* CARD PRINCIPAL */
        .glass-container {
            background: rgba(255, 255, 255, 0.78);
            backdrop-filter: blur(18px);
            -webkit-backdrop-filter: blur(18px);

            border-radius: 28px;
            padding: 2.2rem;

            box-shadow:
                0 30px 60px rgba(90, 120, 200, 0.25),
                inset 0 0 0 1px rgba(255, 255, 255, 0.45);
        }

        /* TÍTULO */
        .page-title {
            font-weight: 800;
            letter-spacing: 0.8px;
            margin-bottom: 2rem;

            background: linear-gradient(
                90deg,
                #5f7cff,
                #7aa2ff,
                #9bbcff
            );
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* TABELA */
        .table {
            border-collapse: separate;
            border-spacing: 0 14px;
        }

        .table thead th {
            border: none;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.9px;
            color: #6d7fb8;
        }

        .table tbody tr {
            background: rgba(255, 255, 255, 0.92);
            border-radius: 18px;
            box-shadow: 0 10px 25px rgba(120, 150, 255, 0.18);
            transition: transform 0.25s ease, box-shadow 0.25s ease;
        }

        .table tbody tr:hover {
            transform: translateY(-3px) scale(1.002);
            box-shadow: 0 18px 40px rgba(120, 150, 255, 0.32);
        }

        .table tbody td {
            border: none;
            padding: 0.95rem;
            vertical-align: middle;
            color: #3c4a7a;
        }

        .table tbody tr td:first-child {
            border-radius: 18px 0 0 18px;
        }

        .table tbody tr td:last-child {
            border-radius: 0 18px 18px 0;
        }

        /* BADGE ID */
        .badge-id {
            background: linear-gradient(
                135deg,
                #c7d6ff,
                #b1c4ff
            );
            color: #3f57a8;
            padding: 0.3rem 0.65rem;
            border-radius: 12px;
            font-size: 0.72rem;
            font-weight: 700;
        }

        /* PLATAFORMA */
        .badge-platform {
            background: linear-gradient(
                135deg,
                #b9e7ff,
                #d4f1ff
            );
            color: #1f5f85;
            font-size: 0.7rem;
            font-weight: 600;
            border-radius: 12px;
            padding: 0.35rem 0.7rem;
        }

        /* DATAS */
        .data-column {
            font-size: 0.72rem;
            color: #7b8fce;
        }

        /* BOTÃO */
        .btn-soft {
            background: linear-gradient(
                135deg,
                #7aa2ff,
                #a7c4ff,
                #c2ddff
            );
            border: none;
            border-radius: 16px;
            padding: 0.55rem 1.4rem;
            font-size: 0.85rem;
            font-weight: 600;
            color: #243b7a;
            box-shadow: 0 10px 28px rgba(120, 150, 255, 0.35);
            transition: all 0.25s ease;
        }

        .btn-soft:hover {
            transform: translateY(-2px);
            box-shadow: 0 16px 40px rgba(120, 150, 255, 0.55);
            color: #1e2f63;
        }
    </style>
</head>

<body>

<div class="container-fluid glass-container">

    <h1 class="page-title">Jogos — Dados Completos</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID Jogo</th>
                <th>Título</th>
                <th>Plataforma</th>
                <th>ID Amigo</th>
                <th>Dono</th>
                <th>Criado em</th>
                <th>Atualizado em</th>
            </tr>
        </thead>
        <tbody>
            <% jogos.forEach(jogo => { %>
            <tr>
                <td><span class="badge-id">#<%= jogo.id %></span></td>
                <td><strong><%= jogo.titulo %></strong></td>
                <td>
                    <span class="badge-platform"><%= jogo.plataforma %></span>
                </td>
                <td><%= jogo.amigoId %></td>
                <td>
                    <% if(jogo.dono) { %>
                        <strong><%= jogo.dono.nome %></strong><br>
                        <small class="text-muted"><%= jogo.dono.email %></small>
                    <% } else { %>
                        <span class="text-danger">Sem dono</span>
                    <% } %>
                </td>
                <td class="data-column">
                    <%= new Date(jogo.createdAt).toLocaleString('pt-BR') %>
                </td>
                <td class="data-column">
                    <%= new Date(jogo.updatedAt).toLocaleString('pt-BR') %>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>

    <div class="mt-4">
        <a href="/jogos" class="btn-soft">← Voltar</a>
    </div>

</div>

</body>
</html>
