<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Jogos • Dados Completos</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
    * {
        font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }

    /* FUNDO — MESMO DA PÁGINA PRINCIPAL */
    body {
        min-height: 100vh;
        padding: 3rem 2rem;

        background: linear-gradient(
            135deg,
            #fff1c1,
            #ffd6a5,
            #ffeadb
        );
        background-size: 300% 300%;
        animation: gradientMove 12s ease infinite;
    }

    @keyframes gradientMove {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    /* CARD */
    .glass-container {
        background: rgba(255, 255, 255, 0.75);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);

        border-radius: 28px;
        padding: 2.2rem;

        box-shadow:
            0 25px 55px rgba(255, 180, 90, 0.28),
            inset 0 0 0 1px rgba(255, 255, 255, 0.4);
    }

    /* TÍTULO — MESMO GRADIENTE */
    .page-title {
        font-weight: 800;
        letter-spacing: 0.8px;
        margin-bottom: 2rem;

        background: linear-gradient(
            90deg,
            #f6c453,
            #f28b6b
        );
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    /* TABELA */
    .table {
        border-collapse: separate;
        border-spacing: 0 14px;
    }

    .table thead th {
        border: none;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.9px;
        color: #a16207;
    }

    .table tbody tr {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 18px;
        box-shadow: 0 8px 20px rgba(255, 180, 90, 0.22);
        transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .table tbody tr:hover {
        transform: translateY(-3px);
        box-shadow: 0 14px 34px rgba(255, 160, 70, 0.38);
    }

    .table tbody td {
        border: none;
        padding: 0.95rem;
        vertical-align: middle;
        color: #7c2d12;
    }

    /* BADGE ID */
    .badge-id {
        background: linear-gradient(
            135deg,
            #ffe29f,
            #ffd6a5
        );
        color: #92400e;
        padding: 0.3rem 0.65rem;
        border-radius: 12px;
        font-size: 0.72rem;
        font-weight: 700;
    }

    /* PLATAFORMA */
    .badge-platform {
        background: linear-gradient(
            135deg,
            #ffddb0,
            #ffe9c7
        );
        color: #7c2d12;
        font-size: 0.7rem;
        font-weight: 600;
        border-radius: 12px;
        padding: 0.35rem 0.7rem;
    }

    /* DATAS */
    .data-column {
        font-size: 0.72rem;
        color: #9a3412;
    }

    /* BOTÃO — MESMO LARANJA DE DESTAQUE */
    .btn-soft {
        background: linear-gradient(
            135deg,
            #ffb347,
            #ff8c42,
            #ff6f3c
        );
        border: none;
        border-radius: 20px;
        padding: 0.7rem 1.9rem;
        font-size: 0.9rem;
        font-weight: 600;
        color: #ffffff;

        box-shadow:
            0 14px 36px rgba(255,140,66,0.5),
            inset 0 1px 0 rgba(255,255,255,0.35);

        transition:
            background-position 0.4s ease,
            transform 0.3s ease,
            box-shadow 0.3s ease;
    }

    .btn-soft:hover {
        background-position: right center;
        transform: translateY(-2px);
        box-shadow:
            0 20px 48px rgba(255,140,66,0.65),
            inset 0 1px 0 rgba(255,255,255,0.45);
        color: #ffffff;
    }
</style>


</head>

<body>

<div class="container-fluid glass-container">

    <h1 class="page-title">Jogos — Dados Completos</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID Jogo</th>
                <th>Título</th>
                <th>Plataforma</th>
                <th>ID Amigo</th>
                <th>Dono</th>
                <th>Criado em</th>
                <th>Atualizado em</th>
            </tr>
        </thead>
        <tbody>
            <% jogos.forEach(jogo => { %>
            <tr>
                <td><span class="badge-id">#<%= jogo.id %></span></td>
                <td><strong><%= jogo.titulo %></strong></td>
                <td>
                    <span class="badge-platform"><%= jogo.plataforma %></span>
                </td>
                <td><%= jogo.amigoId %></td>
                <td>
                    <% if(jogo.dono) { %>
                        <strong><%= jogo.dono.nome %></strong><br>
                        <small class="text-muted"><%= jogo.dono.email %></small>
                    <% } else { %>
                        <span class="text-danger">Sem dono</span>
                    <% } %>
                </td>
                <td class="data-column">
                    <%= new Date(jogo.createdAt).toLocaleString('pt-BR') %>
                </td>
                <td class="data-column">
                    <%= new Date(jogo.updatedAt).toLocaleString('pt-BR') %>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>

    <div class="mt-4">
        <a href="/jogos" class="btn-soft">← Voltar</a>
    </div>

</div>

</body>
</html>
